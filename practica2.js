//Declarar variables
var numero = parseInt(prompt("Ingrese un número para verificar si es primo:"));

//Metodo para verificar si un numero es primo o no

if (numero/2 && numero/numero) {
    document.write("Es un numero primo");
} else {
    document.write("No es un numero primo");
}
